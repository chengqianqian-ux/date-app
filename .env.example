# 复制为 .env 并按需修改（本地开发用）
# 生产环境在 Render 控制台配置，不要提交 .env

# Postgres 连接串（本地起 docker 后用这个；或填 Render 的外部连接串）
DATABASE_URL=postgres://dateapp:dateapp@localhost:5432/dateapp

# JWT 密钥（本地随便填，生产用 Render 自动生成）
JWT_SECRET=local-dev-secret

# 后端端口
PORT=3001
